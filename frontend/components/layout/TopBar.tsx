export default function TopBar() {
    return (
        <div className="w-full h-16 bg-gray-100 text-black border-b border-gray-300 flex items-center justify-center px-6">
            <span className="text-xl font-semibold animate-pulse">Dashboard</span>
        </div>
    );
}